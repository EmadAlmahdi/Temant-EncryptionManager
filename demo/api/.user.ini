; Applies when served via PHP-FPM (Apache/production) — the built-in `php -S` dev server does
; not read .user.ini, see demo/README.md for its equivalent -d flags.
upload_max_filesize = 12M
post_max_size = 12M
